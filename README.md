# QAOA + ML: предсказание оптимальных углов QAOA по вектору h

Решение задачи «Машинное обучение в квантовых вычислениях» (РКЦ):
для каждого вектора линейных коэффициентов `h` (12-кубитная модель Изинга,
фиксированная матрица `J`) предсказать углы QAOA глубины p=5 — по 5 углов
`gamma` и `beta` — максимизирующие вероятность основного состояния
`P(ground)`. Итоговая метрика — среднее `P(ground)` по 500 векторам `h_test`.

## Данные

| файл | что это |
|---|---|
| `J.npy` | матрица квадратичных взаимодействий (12, 12), фиксирована. Точная зеркальная симметрия `i <-> 11-i` |
| `h_train.npy` | обучающая выборка (500, 12), векторы линейных коэффициентов |
| `h_test.npy` | тестовая выборка (500, 12), выдаётся 15.09 до 18:00 МСК |
| `QAOA.py` | дифференцируемый симулятор QAOA из условия (использован без изменений) |
| `submission.csv` | формат посылки: `id, gamma_0..4, beta_0..4` |

## Подход (кратко)

1. **Метки.** Для 500 тренировочных `h` — поинстансная оптимизация углов
   (Adam, 3 рестарта x 250 шагов, все 500 инстансов одним батчем).
   `data/labels.npz` — углы и per-instance `P(ground)` («потолок» для
   каждого инстанса).
2. **Признаки.** `h` + симметризованные/антисимметризованные проекции по
   зеркальной симметрии `J` + физические признаки точного перебора
   2^12 = 4096 конфигураций Изинга: энергия основного состояния, зазор,
   конфигурация, `h·s*`, амплитуда поля.
3. **Модель.** MLP-ствол (3x256, GELU, dropout) с:
   * по одной голове на каждый из 5 слоёв QAOA (gamma и beta — как «schedule»);
   * multi-task aux-головами (энергия и конфигурация основного состояния).
4. **Обучение.** Оптимальные углы **неуникальны** (несколько локальных
   минимумов с сопоставимым P(ground)), поэтому вместо L2-регрессии на
   метки — **end-to-end обучение через сам дифференцируемый симулятор** с потерей
   `-mean P(ground)(h, f(h)) + 0.05·aux`. Данные: 450 реальных + 1000
   синтетических `h ~ U[-1,1]^12`; валидация: 50 реальных (holdout) +
   500 синтетических. Головы углов инициализированы средними метками.
5. **Инференс — «тёплый старт + сильная полировка».** `f(h)` даёт стартовые
   углы, затем **поинстансная много-рестарт оптимизация** (батч = все 500):
   рестарт 0 — предсказание сети, рестарт 1 — сеть + шум, остальные —
   случайные в полном диапазоне; 300 шагов Adam + доводка мелким lr;
   для каждого инстанса оставляем лучший набор углов по P(ground).
   Тысячи итераций классического цикла QAOA → десятки; на GPU (Colab)
   ~2-5 мин на 500 инстансов (лимит — 10 мин).

## Запуск

### Google Colab (для подачи)
Откройте `solution.ipynb` в Colab: всё необходимое (зависимости, `J`,
`h_train`, QAOA-класс) встроено в ноутбук.
1. Приложите `h_test.npy` в рабочую папку (в день выдачи);
2. `Run → Run all`;
3. получите `submission.csv` (последняя ячейка), `data/model.pt`,
   `data/labels.npz`.

Без `h_test.npy` ноутбук обучает модель и сам проверяет на `h_train`
(значения, совпадающие с публичным лидербордом).

### Локально
```bash
pip install -r requirements.txt
cd solution

python generate_labels.py            # метки, ~40 мин CPU / ~1 мин GPU
python train.py                      # end-to-end обучение, ~3 ч CPU / ~10 мин GPU
python infer.py --h h_train.npy --out sub_selfcheck.csv --profile fast   # самопроверка (~15 мин CPU)
python infer.py --h h_test.npy --out submission.csv                     # финал (полная полировка; на CPU ~2-4 ч, на GPU минуты)
python infer.py --h h_test.npy --out submission_brain.csv --profile brain  # «мозг» (MCTech-подход, GPU ~8-9 мин)
python infer.py --h h_train.npy --out sub_heavy.csv --profile brain --pop 12 --gens 3 --steps 400 --refine-iters 50 --seed 7  # «тяжёлый» оффлайн-раунд (~45 мин GPU; brain.pkl накапливается между раундами — меняйте --seed)
python evaluate.py --angles data/labels.npz --h h_train.npy             # проверка углов
```

> **Совет:** финальный `infer.py` с `--profile full` на CPU занимает часы —
> для посылки запускайте его в Google Colab (ноутбук `solution.ipynb`),
> где на GPU весь прогон занимает ~10-15 минут.

## Результаты

Локальный прогон (GPU, torch CUDA):

| конфигурация | P(ground), mean |
|---|---|
| полная поинстансная оптимизация меток (3x250 Adam, «потолок» baseline) | 0.2063 (median 0.1813, max 0.893) |
| сеть (чистая), валидация (holdout 50 + синтетика 500) | 0.1972 |
| сеть + полировка 30 шагов, holdout 50 real | 0.2093 |
| самопроверка на всех 500 h_train: чистая сеть / сеть+полировка (fast) | 0.1890 / 0.2010 |

Время инференса (500 h, сеть + fast-полировка 3x100, GPU): ~48 с;
full-полировка (8x300 + доводка): ~8 мин (лимит задачи — 10 мин).

Проверено на публичном лидерборде: загрузка нашего `submission.csv`
(самопроверка full-прогона) дала **0.2019234299659729** — ровно среднее
сырое P(ground) из файла, т.е. **лидерборд = сырое P(ground) на h_train
без нормализации**. Топ (0.9519) недостижим за бюджет инференса — он
набран тяжёлой оффлайн-оптимизацией h_train (лимит 10 минут действует
только для финала на h_test). Отсюда два трека:

* **Публичный лидерборд (h_train):** можно растить оффлайн — «тяжёлые»
  раунды `--profile brain --pop 12 --gens 3 --steps 400` (каждый ~45 мин
  на GPU, `brain.pkl` накапливает опыт между раундами, менять `--seed`).
* **Финал (h_test, ≤10 мин):** стандартные профили `full` / `brain`
  (оба укладываются в лимит с запасом на GPU).

Все сиды фиксированы (SEED=42); чекпоинты: `data/labels.npz`,
`data/model.pt`, `data/history.csv`.

## Воспроизводимость

* Все сиды: `SEED = 42` (`solution/config.py`);
* Чекпоинты: `data/labels.npz` (метки после каждого рестарта),
  `data/model.pt` (лучшая по валидации), `data/history.csv`;
* Версии библиотек: `requirements.txt`;
* `QAOA.py` из условия используется без изменений;
* Углы на инференсе детерминированы: фиксированная модель + фиксированные
  шаги Adam.

## Структура

```
QAOA/
  J.npy, h_train.npy, QAOA.py, submission.csv   # данные из условия
  solution.ipynb        # Colab-ноутбук «в одну кнопку» (для подачи)
  README.md, requirements.txt
  solution/
    config.py           # сиды и гиперпараметры
    features.py         # физические признаки (перебор 2^12, симметрии)
    model.py            # QAOAAngleNet
    generate_labels.py  # поинстансная оптимизация углов (метки)
    train.py            # end-to-end обучение
    infer.py            # инференс + полировка (full/fast/brain) -> submission.csv
    brain.py            # «мозг» — персистентная память схем (аналог MCTech)
    evaluate.py         # оценка P(ground) для набора углов
    build_notebook.py   # пересборка solution.ipynb из скриптов
  data/                 # артефакты (метки, модель, лог)
  explore/bench.py      # исходные замеры стоимости/масштаба метрики
```
